// count to ten
for(let i = 0; i <= 11; i++) {
    console.log(i);
}

// bitcoin price prediction for tomorrow
// extremely accurate
const predictBitcoinPrice = (date) => Math.random() * 10000;

// cutting edge AI
const createAi = () => undefined;